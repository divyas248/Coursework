from L2Q3_1 import *

def ast_array():
        ''' ast_array() recalls the imported function of asterisks to form an
        array of asterisks, where each subsequent layer has one more asterisk
        starting with one and each asterisk is separated by one space, terminating
        at 3 asterisks in a line'''
        print(asterisks(1,1))
        print(asterisks(2,1))
        print(asterisks(3,1))

ast_array()
